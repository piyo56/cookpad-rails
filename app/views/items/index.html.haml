%h1 Listing items

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Price
      %th Image url
      %th
      %th
      %th

  %tbody
    - @items.each do |item|
      %li.item 
        %h2= link_to item.name, item
        %p= item.description

= link_to 'New Item', new_item_path
