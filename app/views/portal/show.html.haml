%h1 My EC web service!!

%ul.items
  - @categories.each do |category|
    %li.category 
      %h2= link_to category.name, category_items_path(category)
      %p
        %img{:src => category.image_url, :height => "200"}
